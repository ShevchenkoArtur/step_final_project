const initialState = {
    todayTasks: [],
    inboxTasks: [],
    archiveTasks: [],
    binTasks: []
}

const TasksReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default TasksReducer