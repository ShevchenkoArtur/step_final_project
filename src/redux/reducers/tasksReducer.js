const initialState = {
    todayTasks: [],
    inboxTasks: []
}

const TasksReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default TasksReducer